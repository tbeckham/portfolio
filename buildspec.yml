version: 0.1

phases:
  build:
    commands:
      - npm install
      - npm run webpack
artifacts:
  files:
    - index.html
    - favicon.ico
    - TonyBeckham-CloudCowboy.pdf
    - styles/*
    - images/*
    - dist/bundle.js
